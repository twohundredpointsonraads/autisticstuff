"""
autisticstuff
-> fastapi: respsys (unified response system and exc handler, APIExceptions and more), utils
-> logs: logging system with interception
-> safe: RetryableClientSession(aiohttp.ClientSession), safe aiogram funcs
-> sqla: layers (BaseMapping, BaseRepository), types (PydanticJSON), utils (update_with_callback, validate_kwargs_for_model)
-> utils: config (AppSettings, SettingsField), enums (for URS), lru (lru-cache wrappers for objects), progbars
"""
